name: Retired one-time site release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: echo "This one-time workflow is retired. Use the Prerender editable site content workflow for future content builds."
